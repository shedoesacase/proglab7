```bash
  --help                   show help
  --dirs arg               select dirs
  --exclude arg            exclude dirs
  --depth arg (=0)         max depth
  --min-size arg (=2)      min size of file
  --mask arg               masks
  --block-size arg (=4096) block size
  --hash arg (=crc32)      hash algorithm
  ```